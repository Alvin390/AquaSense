// formatters — display formatting utilities
// formatTimestamp(iso: string) → "2 hours ago" | "Just now"
// formatPH(value: number) → "7.2 pH"
// formatFloodRisk(pct: number, category: string) → "Moderate Risk — 48%"
// formatWaterLevel(level: string) → "Normal Flow" | "Very Low" | "Flooded"
