// LiveSavedBadge component
// Small badge indicating data freshness:
//   LIVE  → teal dot + "Live" label (data < 36h and network connected)
//   SAVED → amber dot + "Saved data from X hours ago" (offline or stale)
// Never uses the word "cached" — always "saved"
