// waterStore — Zustand store
// State: selectedSourceId, waterSources[], latestReadings{}, aiRecommendations{}
// Actions: setSelectedSource, updateReadings, updateRecommendation, clearAll
// Slice subscriptions only — components select specific fields to avoid re-renders
